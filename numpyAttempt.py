# -*- coding: utf-8 -*-
"""
Created on Sun May 22 00:40:27 2022

@author: macwr
"""

# def mandle(plot, xs, ys, iters):
#     for x in range(len(xs)):
#         for y in range(len(ys)):
#             z = complex(0,0)
#             for i in range(iters):
#                 z = z**2 + complex(xs[x], y[y])
#                 if abs(z) >4:
#                     break
#                 else:
#                     pass
#             plot[y][x] = i


    

# def imageSize(height, width, minX, maxX, minY, maxY):
#     array = np.zeros((height, width))
#     xs = np.linspace(minX, maxX, width)
#     ys = np.linspace(minY, maxY, height)
#     cs = np.meshgrid
#     return array, xs, ys

# plot, xs, ys = imageSize(100, 150, -2, 2, -2, 2)

# image = mandle(plot, xs, ys, 100)